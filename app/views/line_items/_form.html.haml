= simple_form_for(@line_item) do |f|
  = f.error_notification

  .form-inputs
    = f.association :book
    = f.association :cart
    = f.input :quantity
    = f.input :sub_total

  .form-actions
    = f.button :submit
