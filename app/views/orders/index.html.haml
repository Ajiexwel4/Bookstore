%h1 Listing orders

%table
  %thead
    %tr
      %th Number
      %th Complated at
      %th Status
      %th Total
      %th
      %th
      %th

  %tbody
    - @orders.each do |order|
      %tr
        %td= order.number
        %td= order.complated_at
        %td= order.status
        %td= order.total
        %td= link_to 'Show', order
        %td= link_to 'Edit', edit_order_path(order)
        %td= link_to 'Destroy', order, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Order', new_order_path
